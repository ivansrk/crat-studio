# Ф0 «Скелет + деплой» — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Работающий скелет платформы на проде Render: Next.js + Prisma-схема целиком, MDX-движок контента с белым списком и валидацией, заглушки 12 уроков, дизайн-токены, /health, seed v0, CI.

**Architecture:** Тонкие роуты App Router; вся логика контента в `lib/content` (глубокий модуль: снаружи 3 функции, внутри загрузка+валидация+кэш). MDX валидируется на старте обходом AST (remark-mdx), рендерится через `next-mdx-remote/rsc` с явной картой компонентов. Контент кэшируется в `globalThis` на процесс.

**Tech Stack:** Next.js 15 (App Router, TS strict) · Prisma 6 + PostgreSQL · next-mdx-remote v5 + remark-mdx + unist-util-visit · js-yaml · vitest · next/font (Cormorant Garamond, Manrope, JetBrains Mono) · Render (render.yaml) · GitHub Actions.

**Prerequisites:** Node 22 LTS; локальный Postgres для миграций: `docker run -d --name crat-pg -e POSTGRES_PASSWORD=dev -e POSTGRES_DB=crat -p 5432:5432 postgres:16` → `DATABASE_URL=postgresql://postgres:dev@localhost:5432/crat` в `.env`.

**Требования, покрываемые планом:** CNT-01…04, LES-04/15/16 (движок), UX-01…04, UX-07, SEC-01/02/07/08, seed v0, Ф0-критерии из docs/phases.md.

---

### Task 1: Скаффолд Next.js + тулинг

**Files:**
- Create: `package.json`, `tsconfig.json`, `next.config.ts`, `vitest.config.ts`, `app/layout.tsx`, `app/page.tsx` (временная), `.nvmrc`
- Modify: `.gitignore`

- [ ] **Step 1: Scaffold**

```bash
cd /Users/ivan.srk/Downloads/crat-studio
npx create-next-app@latest . --ts --app --eslint --no-tailwind --no-src-dir --import-alias "@/*" --use-npm --yes
```
Expected: файлы созданы рядом с docs/ (existing files не перезаписывает; при конфликте README — оставить наш).

- [ ] **Step 2: Зависимости**

```bash
npm i prisma @prisma/client js-yaml next-mdx-remote @mdx-js/mdx remark remark-mdx unist-util-visit
npm i -D vitest @types/js-yaml tsx
```

- [ ] **Step 3: Скрипты и vitest**

В `package.json` заменить scripts:

```json
"scripts": {
  "dev": "next dev",
  "build": "prisma generate && next build",
  "start": "next start",
  "typecheck": "tsc --noEmit",
  "lint": "next lint",
  "test": "vitest run",
  "seed": "tsx scripts/seed.ts"
}
```

`vitest.config.ts`:

```ts
import { defineConfig } from 'vitest/config'
import path from 'node:path'

export default defineConfig({
  test: { include: ['lib/**/*.test.ts', 'scripts/**/*.test.ts'] },
  resolve: { alias: { '@': path.resolve(__dirname) } },
})
```

`.nvmrc`: `22`

В `.gitignore` дописать: `coverage/`, `*.tsbuildinfo`, `.env`.

- [ ] **Step 4: Проверка**

```bash
npm run typecheck && npm run lint && npm run build
```
Expected: все зелёные (дефолтное приложение).

- [ ] **Step 5: Commit**

```bash
git add -A && git commit -m "Ф0: скаффолд Next.js 15 + vitest + скрипты"
```

---

### Task 2: Дизайн-токены, шрифты, i18n-словарь

**Files:**
- Create: `lib/design/tokens.css`, `lib/i18n/ru.ts`, `lib/i18n/index.ts`, `app/fonts.ts`
- Modify: `app/layout.tsx`, `app/globals.css`

- [ ] **Step 1: Токены (UX-03) — `lib/design/tokens.css`**

```css
:root {
  --bg-deep: #0E0B0B;
  --red-sky: #7E2F2A;
  --red-sky-2: #A03B2E;
  --neon: #FF4B3A;
  --neon-glow: 0 0 24px rgba(255, 75, 58, .45);
  --mint: #7FD6B4;
  --lavender: #B9A7D6;
  --paper: #F2E9DC;
  --ink: #1A1614;
  --fs-base: 19px;      /* UX-01: 18–20px */
  --fs-small: 17px;     /* «мелкого» текста нет */
  --maxw-text: 46rem;
}
```

- [ ] **Step 2: Шрифты (UX-04) — `app/fonts.ts`**

```ts
import { Cormorant_Garamond, Manrope, JetBrains_Mono } from 'next/font/google'

export const accent = Cormorant_Garamond({ subsets: ['cyrillic', 'latin'], weight: ['500', '700'], variable: '--font-accent' })
export const body = Manrope({ subsets: ['cyrillic', 'latin'], variable: '--font-body' })
export const mono = JetBrains_Mono({ subsets: ['cyrillic', 'latin'], variable: '--font-mono' })
```

- [ ] **Step 3: `app/globals.css`** (заменить содержимое)

```css
@import '../lib/design/tokens.css';

* { box-sizing: border-box; }
html { font-size: var(--fs-base); }
body {
  margin: 0;
  background: var(--bg-deep);
  color: var(--paper);
  font-family: var(--font-body), system-ui, sans-serif;
  line-height: 1.6;
}
h1, h2, h3 { font-family: var(--font-accent), serif; line-height: 1.2; }
a { color: var(--neon); }
main { max-width: var(--maxw-text); margin: 0 auto; padding: 2rem 1rem; }
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after { animation: none !important; transition: none !important; }
}
```

- [ ] **Step 4: Словарь (UX-02) — `lib/i18n/ru.ts`**

Все строки Ф0 (дополняется по мере задач; строк в разметке НЕТ):

```ts
export const ru = {
  site: { title: 'CRAT studio', courseListTitle: 'Курс' },
  lesson: {
    videoSoon: 'Видео скоро появится',
    downloadCheatsheet: 'Скачать шпаргалку',
    finishLesson: 'Завершить урок',
    unavailable: 'Урок временно недоступен, мы уже чиним',
    trainerSoon: 'Тренажёр появится на этом месте',
  },
  health: { ok: 'ok', contentValid: 'контент валиден', contentErrors: 'ошибки контента' },
} as const
```

`lib/i18n/index.ts`:

```ts
import { ru } from './ru'
export const t = ru // MVP: только русский; смена языка = замена импорта
```

- [ ] **Step 5: `app/layout.tsx`** (заменить)

```tsx
import type { Metadata } from 'next'
import { accent, body, mono } from './fonts'
import { t } from '@/lib/i18n'
import './globals.css'

export const metadata: Metadata = { title: t.site.title }

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="ru" className={`${accent.variable} ${body.variable} ${mono.variable}`}>
      <body>{children}</body>
    </html>
  )
}
```

- [ ] **Step 6: Проверить и закоммитить**

```bash
npm run dev &  # открыть localhost:3000 — тёмный фон, кегль 19px
npm run typecheck && git add -A && git commit -m "Ф0: дизайн-токены, шрифты, словарь i18n (UX-01…04)"
```

---

### Task 3: Prisma-схема целиком + первая миграция

**Files:**
- Create: `prisma/schema.prisma`, `prisma/migrations/*` (генерируется), `lib/db.ts`

- [ ] **Step 1: `prisma/schema.prisma`**

Перенести схему из docs/data-model.md **дословно** (все enums: Role, RegistrationStatus, ConsentType, ConsentSource, SubmissionStatus, CertificateStatus, EmailType, EmailStatus; все модели: User, Registration, Consent, Enrollment, MagicLink, LessonProgress, QuizResult, DeferredQuizState, Submission, Certificate, CertificateCounter, EmailLog, TrainerUsage) с шапкой:

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

Сверить каждую модель с docs/data-model.md построчно — это источник правды; расхождение = баг плана.

- [ ] **Step 2: `lib/db.ts`** (singleton для dev hot-reload)

```ts
import { PrismaClient } from '@prisma/client'

const g = globalThis as unknown as { prisma?: PrismaClient }
export const db = g.prisma ?? new PrismaClient()
if (process.env.NODE_ENV !== 'production') g.prisma = db
```

- [ ] **Step 3: Миграция**

```bash
npx prisma migrate dev --name init
```
Expected: `prisma/migrations/<ts>_init/migration.sql` создан, «Your database is now in sync».

- [ ] **Step 4: Commit**

```bash
git add prisma lib/db.ts && git commit -m "Ф0: Prisma-схема по docs/data-model.md + init-миграция"
```

---

### Task 4: lib/content — типы и загрузчик course.yaml (TDD)

**Files:**
- Create: `lib/content/types.ts`, `lib/content/loader.ts`, `lib/content/loader.test.ts`, `lib/content/fixtures/` (тестовый мини-курс)

- [ ] **Step 1: `lib/content/types.ts`**

```ts
export type QuizQuestion = { question: string; options: string[]; correct: number; explanation: string }
export type Quiz = { questions: QuizQuestion[]; deferred?: QuizQuestion[] }
export type LessonMeta = {
  id: string; title: string; video_id?: string
  duration_min?: number; cheatsheet?: boolean; mission_prompt?: boolean
}
export type LessonRef = { id: string; title: string }
export type CourseYaml = { slug: string; title: string; modules: { id: number; title: string; lessons: LessonRef[] }[] }
export type Lesson = {
  meta: LessonMeta; moduleId: number
  dir: string            // абсолютный путь каталога урока
  mdx: string            // сырой lesson.mdx
  quiz: Quiz; practiceMd: string; hasCheatsheet: boolean
}
export type ContentIssue = { level: 'error' | 'warning'; lessonId?: string; message: string }
export type CourseContent = {
  course: CourseYaml
  lessons: Map<string, Lesson>   // только валидные; битые перечислены в issues
  issues: ContentIssue[]
  loadedAt: Date
}
```

- [ ] **Step 2: Фикстуры** — `lib/content/fixtures/valid-course/` с course.yaml (1 модуль, 1 урок `1.1`) и полным каталогом урока (meta.yaml, lesson.mdx `## Привет`, quiz.yaml с 3 валидными вопросами, practice.md); и `lib/content/fixtures/broken/` — course.yaml ссылается на урок `1.2` без каталога, у `1.1` в quiz.yaml 2 вопроса.

valid quiz.yaml фикстура:

```yaml
questions:
  - question: "Что такое ИИ?"
    options: ["Программа", "Магия"]
    correct: 0
    explanation: "Программа, обученная на данных."
  - question: "Вопрос 2?"
    options: ["А", "Б", "В"]
    correct: 1
    explanation: "Потому что Б."
  - question: "Вопрос 3?"
    options: ["Да", "Нет"]
    correct: 0
    explanation: "Да."
```

- [ ] **Step 3: Failing-тест `lib/content/loader.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import path from 'node:path'
import { loadCourse } from './loader'

const fx = (n: string) => path.join(__dirname, 'fixtures', n)

describe('loadCourse', () => {
  it('загружает валидный курс без ошибок', () => {
    const c = loadCourse(fx('valid-course'))
    expect(c.issues.filter(i => i.level === 'error')).toHaveLength(0)
    expect(c.lessons.get('1.1')?.meta.title).toBeTruthy()
  })
  it('битый курс: урок без каталога и квиз ≠3 вопросов — ошибки, не исключения', () => {
    const c = loadCourse(fx('broken'))
    const msgs = c.issues.filter(i => i.level === 'error').map(i => i.message).join('\n')
    expect(msgs).toMatch(/1\.2/)        // нет каталога
    expect(msgs).toMatch(/3 вопроса/)   // квиз
    expect(c.lessons.has('1.1')).toBe(false) // битый урок недоступен
  })
  it('несуществующий каталог курса — одна ошибка, пустой курс', () => {
    const c = loadCourse(fx('nope'))
    expect(c.issues[0].level).toBe('error')
    expect(c.lessons.size).toBe(0)
  })
})
```

- [ ] **Step 4: Убедиться, что тест падает**

```bash
npm test
```
Expected: FAIL — `Cannot find module './loader'`.

- [ ] **Step 5: Реализация `lib/content/loader.ts`**

```ts
import fs from 'node:fs'
import path from 'node:path'
import yaml from 'js-yaml'
import type { CourseContent, CourseYaml, ContentIssue, Lesson, LessonMeta, Quiz } from './types'

// Правила валидации: docs/content-format.md §6. Собираем ВСЕ ошибки, не бросаем исключений.
export function loadCourse(courseDir: string): CourseContent {
  const issues: ContentIssue[] = []
  const lessons = new Map<string, Lesson>()
  const empty = { slug: '', title: '', modules: [] } as CourseYaml

  const courseYamlPath = path.join(courseDir, 'course.yaml')
  if (!fs.existsSync(courseYamlPath)) {
    issues.push({ level: 'error', message: `course.yaml не найден: ${courseYamlPath}` })
    return { course: empty, lessons, issues, loadedAt: new Date() }
  }
  let course: CourseYaml
  try {
    course = yaml.load(fs.readFileSync(courseYamlPath, 'utf8')) as CourseYaml
  } catch (e) {
    issues.push({ level: 'error', message: `course.yaml не парсится: ${(e as Error).message}` })
    return { course: empty, lessons, issues, loadedAt: new Date() }
  }
  if (course.slug !== path.basename(courseDir))
    issues.push({ level: 'error', message: `slug "${course.slug}" ≠ каталогу "${path.basename(courseDir)}"` })

  const seen = new Set<string>()
  for (const mod of course.modules ?? []) {
    for (const ref of mod.lessons ?? []) {
      if (seen.has(ref.id)) { issues.push({ level: 'error', message: `дубль id урока ${ref.id}` }); continue }
      seen.add(ref.id)
      const lesson = loadLesson(courseDir, mod.id, ref.id, issues)
      if (lesson) lessons.set(ref.id, lesson)
    }
  }
  return { course, lessons, issues, loadedAt: new Date() }
}

function loadLesson(courseDir: string, moduleId: number, id: string, issues: ContentIssue[]): Lesson | null {
  const err = (message: string) => { issues.push({ level: 'error', lessonId: id, message }) }
  const dir = path.join(courseDir, `module-${moduleId}`, `lesson-${id}`)
  if (!fs.existsSync(dir)) { err(`каталог урока ${id} не найден: ${dir}`); return null }

  const before = issues.filter(i => i.level === 'error').length
  const meta = readYaml<LessonMeta>(path.join(dir, 'meta.yaml'), err)
  const mdx = readFile(path.join(dir, 'lesson.mdx'), err)
  const quiz = readYaml<Quiz>(path.join(dir, 'quiz.yaml'), err)
  const practiceMd = readFile(path.join(dir, 'practice.md'), err)

  if (meta && meta.id !== id) err(`meta.id "${meta.id}" ≠ id урока "${id}"`)
  if (quiz) validateQuiz(quiz, err)
  const hasCheatsheet = fs.existsSync(path.join(dir, 'cheatsheet.pdf'))
  if (meta?.cheatsheet && !hasCheatsheet) err(`cheatsheet: true, а cheatsheet.pdf отсутствует`)
  if (meta && !meta.video_id) issues.push({ level: 'warning', lessonId: id, message: 'пустой video_id (LES-03, штатно)' })

  if (issues.filter(i => i.level === 'error').length > before) return null
  return { meta: meta!, moduleId, dir, mdx: mdx!, quiz: quiz!, practiceMd: practiceMd!, hasCheatsheet }
}

function validateQuiz(q: Quiz, err: (m: string) => void) {
  const blocks: [string, typeof q.questions | undefined][] = [['questions', q.questions], ['deferred', q.deferred]]
  for (const [name, list] of blocks) {
    if (!list) continue
    if (name === 'questions' && list.length !== 3) err(`в quiz.yaml не ровно 3 вопроса (${list.length})`)
    list.forEach((it, i) => {
      if (!it.question?.trim() || !it.explanation?.trim()) err(`${name}[${i}]: пустой question/explanation`)
      if (!it.options || it.options.length < 2 || it.options.length > 4) err(`${name}[${i}]: вариантов должно быть 2–4`)
      else if (it.correct < 0 || it.correct >= it.options.length) err(`${name}[${i}]: correct вне диапазона`)
    })
  }
}

function readYaml<T>(p: string, err: (m: string) => void): T | null {
  const raw = readFile(p, err)
  if (raw === null) return null
  try { return yaml.load(raw) as T } catch (e) { err(`${path.basename(p)} не парсится: ${(e as Error).message}`); return null }
}
function readFile(p: string, err: (m: string) => void): string | null {
  if (!fs.existsSync(p)) { err(`отсутствует обязательный файл ${path.basename(p)}`); return null }
  return fs.readFileSync(p, 'utf8')
}
```

- [ ] **Step 6: Тесты зелёные**

```bash
npm test
```
Expected: PASS (3 теста).

- [ ] **Step 7: Commit**

```bash
git add lib/content && git commit -m "Ф0: lib/content загрузчик + валидация course.yaml/quiz.yaml (CNT-02, CNT-03)"
```

---

### Task 5: Валидация MDX — белый список (TDD)

**Files:**
- Create: `lib/content/validate-mdx.ts`, `lib/content/validate-mdx.test.ts`, `lib/content/whitelist.ts`

- [ ] **Step 1: `lib/content/whitelist.ts`** (единственный источник правды о компонентах — используется и валидатором, и рендером)

```ts
export const MDX_COMPONENTS = ['Figure', 'Gallery', 'Callout', 'Video', 'Download', 'Trainer', 'Animation', 'Divider'] as const
export type MdxComponentName = (typeof MDX_COMPONENTS)[number]
export const TRAINER_IDS = ['t1', 't2', 't3'] as const
export const FORBIDDEN_HTML = /<\s*(script|iframe|style)\b/i
```

- [ ] **Step 2: Failing-тесты `lib/content/validate-mdx.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { validateMdx } from './validate-mdx'

const ANIMS = ['neon-pulse', 'float-up']
const ok = (src: string, assets: string[] = []) =>
  validateMdx(src, { existingAssets: new Set(assets), animationIds: new Set(ANIMS) })

describe('validateMdx', () => {
  it('обычный markdown + компоненты белого списка — ок', () => {
    const r = ok('## Заголовок\n\n<Callout type="idea">Мысль</Callout>\n\n<Divider />')
    expect(r).toHaveLength(0)
  })
  it('import/export запрещены', () => {
    expect(ok('import x from "y"\n\nтекст').join()).toMatch(/import|export|JS/i)
  })
  it('JS-выражения {…} запрещены', () => {
    expect(ok('Счёт: {1 + 1}').join()).toMatch(/выражен/i)
  })
  it('компонент вне белого списка — ошибка', () => {
    expect(ok('<Hacker />').join()).toMatch(/Hacker/)
  })
  it('script/iframe/style в raw HTML — ошибка', () => {
    expect(ok('<script>alert(1)</script>').join()).toMatch(/script/i)
  })
  it('Trainer с неизвестным id — ошибка', () => {
    expect(ok('<Trainer id="t9" />').join()).toMatch(/t9/)
  })
  it('Animation с неизвестным id — ошибка; с известным — ок', () => {
    expect(ok('<Animation id="wat" />').join()).toMatch(/wat/)
    expect(ok('<Animation id="neon-pulse" />')).toHaveLength(0)
  })
  it('Figure/Download/![…] на несуществующий asset — ошибка; на существующий — ок', () => {
    expect(ok('<Figure src="assets/no.png" />').join()).toMatch(/no\.png/)
    expect(ok('<Figure src="assets/yes.png" />', ['assets/yes.png'])).toHaveLength(0)
    expect(ok('![схема](assets/no2.png)').join()).toMatch(/no2\.png/)
  })
  it('битый MDX-синтаксис — ошибка, не исключение', () => {
    expect(ok('<Callout>').length).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 3: Убедиться, что падают**

```bash
npm test
```
Expected: FAIL — `Cannot find module './validate-mdx'`.

- [ ] **Step 4: Реализация `lib/content/validate-mdx.ts`**

```ts
import { remark } from 'remark'
import remarkMdx from 'remark-mdx'
import { visit } from 'unist-util-visit'
import { MDX_COMPONENTS, TRAINER_IDS, FORBIDDEN_HTML } from './whitelist'

export type MdxValidationCtx = { existingAssets: Set<string>; animationIds: Set<string> }

/** Возвращает список ошибок (пустой = валидно). Правила: content-format.md §4, §6; LES-15. */
export function validateMdx(src: string, ctx: MdxValidationCtx): string[] {
  const errors: string[] = []
  let tree: ReturnType<typeof remark.prototype.parse>
  try {
    tree = remark().use(remarkMdx).parse(src)
  } catch (e) {
    return [`MDX не парсится: ${(e as Error).message}`]
  }

  const whitelist = new Set<string>(MDX_COMPONENTS)
  const attr = (node: any, name: string): string | undefined =>
    node.attributes?.find((a: any) => a.type === 'mdxJsxAttribute' && a.name === name)?.value

  visit(tree as any, (node: any) => {
    switch (node.type) {
      case 'mdxjsEsm':
        errors.push('import/export (JS) в уроке запрещены'); break
      case 'mdxFlowExpression':
      case 'mdxTextExpression':
        errors.push(`JS-выражение {${node.value}} запрещено`); break
      case 'html':
        if (FORBIDDEN_HTML.test(node.value)) errors.push(`запрещённый HTML-тег: ${node.value.slice(0, 40)}`); break
      case 'image':
        if (!ctx.existingAssets.has(node.url)) errors.push(`картинка ссылается на несуществующий файл: ${node.url}`); break
      case 'mdxJsxFlowElement':
      case 'mdxJsxTextElement': {
        const name = node.name ?? ''
        if (!whitelist.has(name)) { errors.push(`компонент вне белого списка: <${name || '(фрагмент)'}>`); break }
        if (name === 'Trainer') {
          const id = attr(node, 'id')
          if (!id || !(TRAINER_IDS as readonly string[]).includes(id)) errors.push(`<Trainer id="${id}"> — неизвестный тренажёр`)
        }
        if (name === 'Animation') {
          const id = attr(node, 'id')
          if (!id || !ctx.animationIds.has(id)) errors.push(`<Animation id="${id}"> — нет в библиотеке анимаций`)
        }
        if (name === 'Figure' || name === 'Download') {
          const file = attr(node, name === 'Figure' ? 'src' : 'file')
          if (file && file !== 'cheatsheet.pdf' && !ctx.existingAssets.has(file))
            errors.push(`<${name}> ссылается на несуществующий файл: ${file}`)
        }
        break
      }
    }
  })
  return errors
}
```

- [ ] **Step 5: Тесты зелёные**

```bash
npm test
```
Expected: PASS (все, включая Task 4).

- [ ] **Step 6: Commit**

```bash
git add lib/content && git commit -m "Ф0: валидация MDX по белому списку (LES-04, LES-15)"
```

---

### Task 6: Публичный API контента + интеграция валидаций + instrumentation

**Files:**
- Create: `lib/content/index.ts`, `lib/content/index.test.ts`, `instrumentation.ts`
- Modify: `lib/content/loader.ts` (подключить validateMdx + скан assets)

- [ ] **Step 1: В `loader.ts`, в `loadLesson` после чтения mdx добавить**

```ts
import { validateMdx } from './validate-mdx'
import { animationIds } from '@/lib/design/animations/registry'
// …внутри loadLesson, после readFile lesson.mdx (mdx !== null):
if (mdx !== null) {
  const assetsDir = path.join(dir, 'assets')
  const existingAssets = new Set<string>(
    fs.existsSync(assetsDir) ? fs.readdirSync(assetsDir).map(f => `assets/${f}`) : [],
  )
  for (const m of validateMdx(mdx, { existingAssets, animationIds })) err(`lesson.mdx: ${m}`)
}
```

(`registry` появится в Task 7 — на этом шаге создать её заранее минимальной: см. Task 7 Step 1; порядок задач сохраняет сборку зелёной.)

- [ ] **Step 2: `lib/content/index.ts`** — единственная точка чтения контента (CNT-01):

```ts
import path from 'node:path'
import { loadCourse } from './loader'
import type { CourseContent, Lesson } from './types'

const COURSE_SLUG = 'ai-basics'
const g = globalThis as unknown as { __content?: CourseContent }

/** Загружает и валидирует контент один раз на процесс; ошибки — внутрь issues, никогда не бросает. */
export function getContent(): CourseContent {
  if (!g.__content) g.__content = loadCourse(path.join(process.cwd(), 'content', COURSE_SLUG))
  return g.__content
}
export function getLesson(id: string): Lesson | null { return getContent().lessons.get(id) ?? null }
export function contentErrors() { return getContent().issues.filter(i => i.level === 'error') }
```

- [ ] **Step 3: Тест `lib/content/index.test.ts`** — реальный контент репозитория всегda валиден:

```ts
import { describe, it, expect } from 'vitest'
import { getContent, contentErrors } from './index'

describe('контент репозитория', () => {
  it('12 уроков из content/ai-basics валидны', () => {
    expect(contentErrors()).toEqual([])
    expect(getContent().lessons.size).toBe(12)
  })
})
```
(Упадёт до Task 8 — это ожидаемо; запускать после Task 8. Пометить `it.skip` до Task 8 и снять skip там.)

- [ ] **Step 4: `instrumentation.ts`** (корень) — валидация на старте (CNT-02):

```ts
export async function register() {
  if (process.env.NEXT_RUNTIME === 'nodejs') {
    const { getContent } = await import('./lib/content')
    const { issues } = getContent()
    for (const i of issues) console[i.level === 'error' ? 'error' : 'warn'](`[content] ${i.lessonId ?? ''} ${i.message}`)
  }
}
```

- [ ] **Step 5: typecheck + commit**

```bash
npm run typecheck && npm test
git add lib/content instrumentation.ts && git commit -m "Ф0: API контента + валидация на старте (CNT-01, CNT-02)"
```

---

### Task 7: Библиотека анимаций + MDX-компоненты

**Files:**
- Create: `lib/design/animations/registry.ts`, `lib/design/animations/animations.css`, `components/mdx/{Figure,Gallery,Callout,Video,Download,Trainer,Animation,Divider}.tsx`, `components/mdx/index.ts`, `components/mdx/mdx.css`

- [ ] **Step 1: Реестр анимаций (LES-16) — `lib/design/animations/registry.ts`**

```ts
/** id → CSS-класс. Расширение = новая запись + керфреймы в animations.css. */
export const ANIMATIONS: Record<string, string> = {
  'neon-pulse': 'anim-neon-pulse',
  'float-up': 'anim-float-up',
}
export const animationIds = new Set(Object.keys(ANIMATIONS))
```

`lib/design/animations/animations.css` (подключить в globals.css через `@import`):

```css
.anim-neon-pulse { animation: neonPulse 2.4s ease-in-out infinite; }
@keyframes neonPulse { 0%, 100% { text-shadow: none; } 50% { text-shadow: var(--neon-glow); } }
.anim-float-up { animation: floatUp 3s ease-in-out infinite alternate; }
@keyframes floatUp { from { transform: translateY(0); } to { transform: translateY(-8px); } }
/* prefers-reduced-motion гасится глобально в globals.css */
```

- [ ] **Step 2: Компоненты.** Каждый — маленький серверный компонент; классы в `components/mdx/mdx.css` (тёмные врезки, крупные подписи — токены из tokens.css). Полный код:

`Figure.tsx`
```tsx
export function Figure({ src, caption, assetBase }: { src: string; caption?: string; assetBase?: string }) {
  return (
    <figure className="mdx-figure">
      {/* eslint-disable-next-line @next/next/no-img-element */}
      <img src={`${assetBase ?? ''}/${src}`} alt={caption ?? ''} />
      {caption && <figcaption>{caption}</figcaption>}
    </figure>
  )
}
```

`Gallery.tsx`
```tsx
export function Gallery({ children }: { children: React.ReactNode }) {
  return <div className="mdx-gallery">{children}</div>
}
```

`Callout.tsx`
```tsx
import { t } from '@/lib/i18n'
const ICONS = { idea: '💡', warning: '⚠️', example: '🧭' } as const
export function Callout({ type = 'idea', children }: { type?: keyof typeof ICONS; children: React.ReactNode }) {
  return <aside className={`mdx-callout mdx-callout-${type}`} aria-label={type}>{ICONS[type]} {children}</aside>
}
```

`Video.tsx`
```tsx
export function Video({ kinescope }: { kinescope: string }) {
  return (
    <div className="mdx-video">
      <iframe src={`https://kinescope.io/embed/${kinescope}`} allow="autoplay; fullscreen; picture-in-picture; encrypted-media" allowFullScreen />
    </div>
  )
}
```

`Download.tsx`
```tsx
export function Download({ file, children, assetBase }: { file: string; children: React.ReactNode; assetBase?: string }) {
  return <a className="mdx-download" href={`${assetBase ?? ''}/${file}`} download>⬇ {children}</a>
}
```

`Trainer.tsx` (Ф0-заглушка; реальный — Ф4)
```tsx
import { t } from '@/lib/i18n'
export function Trainer({ id }: { id: string; mode?: 'inline' | 'link' }) {
  return <div className="mdx-trainer-stub">🎮 {t.lesson.trainerSoon} ({id})</div>
}
```

`Animation.tsx`
```tsx
import { ANIMATIONS } from '@/lib/design/animations/registry'
export function Animation({ id, children }: { id: string; children?: React.ReactNode }) {
  return <span className={ANIMATIONS[id] ?? ''}>{children ?? '✳'}</span>
}
```

`Divider.tsx`
```tsx
export function Divider() { return <hr className="mdx-divider" /> }
```

`index.ts` — карта для MDXRemote (имена = whitelist.ts; расхождение поймает typecheck ниже):

```ts
import type { MdxComponentName } from '@/lib/content/whitelist'
import { Figure } from './Figure'; import { Gallery } from './Gallery'; import { Callout } from './Callout'
import { Video } from './Video'; import { Download } from './Download'; import { Trainer } from './Trainer'
import { Animation } from './Animation'; import { Divider } from './Divider'

export function mdxComponents(assetBase: string): Record<MdxComponentName, React.ComponentType<any>> {
  return {
    Figure: (p) => <Figure {...p} assetBase={assetBase} />,
    Download: (p) => <Download {...p} assetBase={assetBase} />,
    Gallery, Callout, Video, Trainer, Animation, Divider,
  }
}
```
(файл — `index.tsx`, т.к. JSX.)

`mdx.css` (подключить `@import` в globals.css):

```css
.mdx-figure img { max-width: 100%; border-radius: 8px; }
.mdx-figure figcaption { color: var(--lavender); font-size: var(--fs-small); margin-top: .5rem; }
.mdx-gallery { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 1rem; }
.mdx-callout { border-left: 3px solid var(--neon); background: rgba(255, 75, 58, .07); padding: 1rem 1.25rem; border-radius: 0 8px 8px 0; margin: 1.5rem 0; }
.mdx-callout-warning { border-color: var(--lavender); background: rgba(185, 167, 214, .08); }
.mdx-callout-example { border-color: var(--mint); background: rgba(127, 214, 180, .07); }
.mdx-video { position: relative; aspect-ratio: 16 / 9; }
.mdx-video iframe { position: absolute; inset: 0; width: 100%; height: 100%; border: 0; border-radius: 8px; }
.mdx-download { display: inline-block; border: 1px solid var(--neon); box-shadow: var(--neon-glow); padding: .75rem 1.5rem; border-radius: 8px; text-decoration: none; }
.mdx-trainer-stub { border: 1px dashed var(--lavender); padding: 1.5rem; text-align: center; border-radius: 8px; color: var(--lavender); }
.mdx-divider { border: 0; border-top: 1px solid rgba(242, 233, 220, .2); margin: 2.5rem auto; width: 60%; }
```

- [ ] **Step 3: typecheck + commit**

```bash
npm run typecheck
git add lib/design components && git commit -m "Ф0: библиотека анимаций и MDX-компоненты белого списка (LES-04, LES-16)"
```

---

### Task 8: Заглушки 12 уроков (CNT-04)

**Files:**
- Create: `content/ai-basics/course.yaml`, 12 каталогов `content/ai-basics/module-{1..4}/lesson-*/`, общий placeholder `assets/placeholder.svg` в уроках, где нужен, `cheatsheet.pdf` в уроке 1.2

- [ ] **Step 1: `course.yaml`** (титулы-заглушки; реальные придут из course-factory):

```yaml
slug: ai-basics
title: "Искусственный интеллект в профессиональной и личной деятельности"
modules:
  - id: 1
    title: "Модуль 1. Знакомство с ИИ"
    lessons:
      - { id: "1.1", title: "Что такое нейросеть" }
      - { id: "1.2", title: "Первый диалог с ИИ" }
      - { id: "1.3", title: "Как формулировать запросы" }
  - id: 2
    title: "Модуль 2. ИИ в работе"
    lessons:
      - { id: "2.1", title: "Тексты и письма" }
      - { id: "2.2", title: "Таблицы и данные" }
      - { id: "2.3", title: "Презентации" }
  - id: 3
    title: "Модуль 3. ИИ в жизни"
    lessons:
      - { id: "3.1", title: "Изображения" }
      - { id: "3.2", title: "Планирование и быт" }
      - { id: "3.3", title: "Обучение с ИИ" }
  - id: 4
    title: "Модуль 4. Щит"
    lessons:
      - { id: "4.1", title: "Правда или нейросеть" }
      - { id: "4.2", title: "Безопасность данных" }
      - { id: "4.3", title: "Твой ИИ-набор" }
```

- [ ] **Step 2: Каталог каждого урока** — meta.yaml (id, title из course.yaml, `video_id: ""`), quiz.yaml (3 осмысленных вопроса-заглушки по теме урока, формат из Task 4 Step 2), practice.md (одно действие), lesson.mdx (2–4 абзаца осмысленного русского текста-заглушки по теме + компоненты). Распределение компонентов, чтобы **каждый встретился хотя бы раз** (критерий CNT-04):
  - 1.1: `mission_prompt: true` в meta; `<Callout type="idea">` + `<Animation id="neon-pulse" />`
  - 1.2: `cheatsheet: true` + cheatsheet.pdf + `<Download file="cheatsheet.pdf">Шпаргалка</Download>` + `<Divider />`
  - 1.3: `<Figure src="assets/placeholder.svg" caption="Схема запроса" />` (+файл assets/placeholder.svg)
  - 2.1: `<Callout type="warning">`; 2.2: `<Gallery>` с двумя `<Figure>` (+assets); 2.3: `<Video kinescope="placeholder" />`
  - 3.1: `<Animation id="float-up" />`; 3.2: `<Callout type="example">`; 3.3: `<Divider />`
  - 4.1: `<Trainer id="t2" />`; 4.2: `<Callout type="warning">`; 4.3: `<Trainer id="t1" />` + `<Trainer id="t3" mode="link" />`

`assets/placeholder.svg` (один и тот же файл копируется в уроки 1.3 и 2.2):

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="640" height="360"><rect width="640" height="360" fill="#1A1614"/><text x="320" y="180" fill="#B9A7D6" font-size="28" text-anchor="middle">Иллюстрация появится здесь</text></svg>
```

`cheatsheet.pdf`-пустышка (минимальный валидный PDF):

```bash
printf '%%PDF-1.4\n1 0 obj<</Type/Catalog/Pages 2 0 R>>endobj\n2 0 obj<</Type/Pages/Kids[3 0 R]/Count 1>>endobj\n3 0 obj<</Type/Page/Parent 2 0 R/MediaBox[0 0 595 842]>>endobj\nxref\n0 4\ntrailer<</Size 4/Root 1 0 R>>\n%%%%EOF\n' > content/ai-basics/module-1/lesson-1.2/cheatsheet.pdf
```

- [ ] **Step 3: Снять `it.skip`** в `lib/content/index.test.ts` (Task 6 Step 3) и прогнать:

```bash
npm test
```
Expected: PASS — «12 уроков валидны», ноль ошибок.

- [ ] **Step 4: Commit**

```bash
git add content lib/content && git commit -m "Ф0: заглушки 12 уроков, каждый MDX-компонент задействован (CNT-04)"
```

---

### Task 9: Страницы — список курса, урок, раздача ассетов

**Files:**
- Create: `app/page.tsx` (заменить), `app/app/lessons/[lessonId]/page.tsx`, `app/content-assets/[...path]/route.ts`
- Modify: `lib/i18n/ru.ts` (строки страниц)

- [ ] **Step 1: Главная Ф0** (временная — список модулей/уроков из контента; настоящая главная — Ф1) — `app/page.tsx`:

```tsx
import Link from 'next/link'
import { getContent } from '@/lib/content'
import { t } from '@/lib/i18n'

export default function Home() {
  const { course } = getContent()
  return (
    <main>
      <h1>{t.site.title}</h1>
      <h2>{course.title}</h2>
      {course.modules.map(m => (
        <section key={m.id}>
          <h3>{m.title}</h3>
          <ul>{m.lessons.map(l => (
            <li key={l.id}><Link href={`/app/lessons/${l.id}`}>{l.id} · {l.title}</Link></li>
          ))}</ul>
        </section>
      ))}
    </main>
  )
}
```

- [ ] **Step 2: Страница урока** — `app/app/lessons/[lessonId]/page.tsx` (Ф0: без auth-гейта — LES-06 включается в Ф1 вместе с сессиями; квиз-шаг — Ф2):

```tsx
import { MDXRemote } from 'next-mdx-remote/rsc'
import { getLesson } from '@/lib/content'
import { mdxComponents } from '@/components/mdx'
import { t } from '@/lib/i18n'

export default async function LessonPage({ params }: { params: Promise<{ lessonId: string }> }) {
  const { lessonId } = await params
  const lesson = getLesson(lessonId)
  if (!lesson) return <main><p>{t.lesson.unavailable}</p></main>
  const assetBase = `/content-assets/ai-basics/module-${lesson.moduleId}/lesson-${lesson.meta.id}`
  return (
    <main>
      <h1>{lesson.meta.title}</h1>
      {lesson.meta.video_id
        ? <div className="mdx-video"><iframe src={`https://kinescope.io/embed/${lesson.meta.video_id}`} allowFullScreen /></div>
        : <p className="mdx-trainer-stub">{t.lesson.videoSoon}</p>}
      <MDXRemote source={lesson.mdx} components={mdxComponents(assetBase)} />
      {lesson.hasCheatsheet && <a className="mdx-download" href={`${assetBase}/cheatsheet.pdf`} download>{t.lesson.downloadCheatsheet}</a>}
      <p><button className="mdx-download" disabled>{t.lesson.finishLesson}</button></p>
    </main>
  )
}
```
(кнопка «Завершить урок» — визуальная заглушка до Ф2.)

- [ ] **Step 3: Раздача ассетов** — `app/content-assets/[...path]/route.ts` (только из content/, без traversal):

```ts
import fs from 'node:fs'
import path from 'node:path'

const ROOT = path.join(process.cwd(), 'content')
const MIME: Record<string, string> = { '.svg': 'image/svg+xml', '.png': 'image/png', '.jpg': 'image/jpeg', '.webp': 'image/webp', '.pdf': 'application/pdf' }

export async function GET(_req: Request, { params }: { params: Promise<{ path: string[] }> }) {
  const { path: parts } = await params
  const target = path.normalize(path.join(ROOT, ...parts))
  if (!target.startsWith(ROOT + path.sep)) return new Response(null, { status: 404 })
  const ext = path.extname(target).toLowerCase()
  if (!MIME[ext] || !fs.existsSync(target)) return new Response(null, { status: 404 })
  return new Response(fs.readFileSync(target), { headers: { 'Content-Type': MIME[ext], 'Cache-Control': 'public, max-age=3600' } })
}
```

- [ ] **Step 4: Ручная проверка**

```bash
npm run dev
```
Открыть `/` → 4 модуля, 12 ссылок; `/app/lessons/1.3` → текст + картинка-заглушка; `/app/lessons/1.2` → кнопка шпаргалки скачивает PDF; несуществующий `/app/lessons/9.9` → «Урок временно недоступен».

- [ ] **Step 5: Commit**

```bash
git add app lib/i18n && git commit -m "Ф0: список курса, страница урока с MDX-рендером, раздача ассетов"
```

---

### Task 10: /health + Plausible

**Files:**
- Create: `app/health/page.tsx`, `app/api/health/route.ts`
- Modify: `app/layout.tsx` (Plausible), `lib/i18n/ru.ts`

- [ ] **Step 1: `app/api/health/route.ts`** (для UptimeRobot; 500 при битом контенте):

```ts
import { contentErrors, getContent } from '@/lib/content'

export const dynamic = 'force-dynamic'
export async function GET() {
  const errors = contentErrors()
  const body = { ok: errors.length === 0, lessons: getContent().lessons.size, errors: errors.map(e => `${e.lessonId ?? ''} ${e.message}`) }
  return Response.json(body, { status: body.ok ? 200 : 500 })
}
```

- [ ] **Step 2: `app/health/page.tsx`** (человекочитаемая — smoke-чеклист Ивана):

```tsx
import { contentErrors, getContent } from '@/lib/content'
import { t } from '@/lib/i18n'

export const dynamic = 'force-dynamic'
export default function Health() {
  const errors = contentErrors()
  return (
    <main>
      <h1>{errors.length === 0 ? `✅ ${t.health.ok}` : `❌ ${t.health.contentErrors}`}</h1>
      <p>{t.health.contentValid}: {getContent().lessons.size}/12</p>
      <ul>{errors.map((e, i) => <li key={i}>{e.lessonId} {e.message}</li>)}</ul>
    </main>
  )
}
```

- [ ] **Step 3: Plausible (UX-07)** — в `app/layout.tsx` внутрь `<html>` перед `<body>`:

```tsx
{process.env.NEXT_PUBLIC_PLAUSIBLE_DOMAIN && (
  <head>
    <script defer data-domain={process.env.NEXT_PUBLIC_PLAUSIBLE_DOMAIN} src="https://plausible.io/js/script.js" />
  </head>
)}
```
и добавить `NEXT_PUBLIC_PLAUSIBLE_DOMAIN=` в `.env.example`.

- [ ] **Step 4: Проверка + commit**

`curl localhost:3000/api/health` → `{"ok":true,"lessons":12,…}`; сломать вручную один quiz.yaml → 500 и список ошибок на /health → починить обратно.

```bash
git add app .env.example lib/i18n && git commit -m "Ф0: /health и /api/health, Plausible (UX-07)"
```

---

### Task 11: seed v0 + синк админов

**Files:**
- Create: `lib/auth/sync-admins.ts`, `scripts/seed.ts`
- Modify: `instrumentation.ts`

- [ ] **Step 1: `lib/auth/sync-admins.ts`** (AUTH-09; идемпотентен):

```ts
import { db } from '@/lib/db'

/** ADMIN_EMAILS="a@b.c, d@e.f" → создаёт/чинит админ-учётки. Понижение ролей не делает: доступ админки проверяется по env на каждый запрос (AUTH-10). */
export async function syncAdmins(): Promise<string[]> {
  const emails = (process.env.ADMIN_EMAILS ?? '').split(',').map(e => e.trim().toLowerCase()).filter(Boolean)
  for (const email of emails) {
    await db.user.upsert({
      where: { email },
      update: { role: 'ADMIN' },
      create: { email, firstName: 'Admin', lastName: email.split('@')[0], role: 'ADMIN' },
    })
  }
  return emails
}
```

- [ ] **Step 2: `scripts/seed.ts`** (v0 — блок Ф0 из docs/seed.md; блоки Ф1+ добавляются в своих фазах):

```ts
import { syncAdmins } from '@/lib/auth/sync-admins'
import { db } from '@/lib/db'

async function main() {
  const admins = await syncAdmins()
  console.log(`[seed v0] админы синхронизированы: ${admins.join(', ') || '(ADMIN_EMAILS пуст)'}`)
}
main().finally(() => db.$disconnect())
```
Для запуска tsx с алиасом `@`: добавить в `package.json` `"seed": "tsx --tsconfig tsconfig.json scripts/seed.ts"` — если алиас не резолвится, заменить импорты на относительные (`../lib/...`), это допустимо.

- [ ] **Step 3: В `instrumentation.ts`** после блока контента добавить:

```ts
try {
  const { syncAdmins } = await import('./lib/auth/sync-admins')
  await syncAdmins()
} catch (e) {
  console.error('[startup] syncAdmins не выполнен (нет БД?):', (e as Error).message)
}
```

- [ ] **Step 4: Проверка + commit**

```bash
ADMIN_EMAILS=test@example.com npm run seed   # → «админы синхронизированы: test@example.com»
npm run seed                                  # повторно — без дублей (upsert)
git add lib/auth scripts instrumentation.ts package.json && git commit -m "Ф0: seed v0 + синк админов из ADMIN_EMAILS (AUTH-09)"
```

---

### Task 12: CI, render.yaml, DECISIONS, финал

**Files:**
- Create: `.github/workflows/ci.yml`, `render.yaml`
- Modify: `DECISIONS.md`, `README.md`

- [ ] **Step 1: `.github/workflows/ci.yml`**

```yaml
name: CI
on: [push, pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: gitleaks/gitleaks-action@v2
        env: { GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}" }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: npm }
      - run: npm ci
      - run: npx prisma generate
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
```

- [ ] **Step 2: `render.yaml`** (Blueprint; SEC-07 — миграции в preDeploy):

```yaml
services:
  - type: web
    name: crat-studio
    runtime: node
    plan: starter
    buildCommand: npm ci && npm run build
    preDeployCommand: npx prisma migrate deploy
    startCommand: npm run start
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase: { name: crat-studio-db, property: connectionString }
      - { key: ADMIN_EMAILS, sync: false }
      - { key: APP_URL, sync: false }
      - { key: SESSION_SECRET, generateValue: true }
      - { key: RESEND_API_KEY, sync: false }
      - { key: ANTHROPIC_API_KEY, sync: false }
      - { key: NEXT_PUBLIC_PLAUSIBLE_DOMAIN, sync: false }
databases:
  - name: crat-studio-db
    plan: basic-256mb
```

- [ ] **Step 3: DECISIONS.md** — новая запись сверху:

```markdown
### D-026. Тулинг Ф0: vitest, next-mdx-remote, instrumentation-хук
**Решение:** тесты — vitest (быстрый, нативный TS); рендер MDX — next-mdx-remote/rsc с явной картой компонентов (белый список из lib/content/whitelist.ts — один источник и для валидатора, и для рендера); валидация контента на старте — instrumentation.ts.
**Причина:** минимальный стек под правило CNT-02 «валидация при старте» в среде Next.js, где нет классического «main()».
**Альтернативы:** @next/mdx (компилирует только импортируемые файлы, не подходит для content/ вне бандла), jest (медленнее, лишняя конфигурация).
```

- [ ] **Step 4: README.md** — раздел «Деплой на Render» (шаги для Ивана): New → Blueprint → указать repo `ivansrk/crat-studio` → Render читает render.yaml → заполнить env (ADMIN_EMAILS, APP_URL, NEXT_PUBLIC_PLAUSIBLE_DOMAIN; остальное авто) → после первого деплоя включить автобэкапы Postgres в настройках базы (руками, SEC-07).

- [ ] **Step 5: Полная проверка и пуш**

```bash
npm run typecheck && npm run lint && npm test && npm run build
git add -A && git commit -m "Ф0: CI, render.yaml, D-026" && git push
```
Expected: CI на GitHub зелёный.

---

## Внешние шаги Ивана (закрытие Ф0 по phases.md)

1. Render → New Blueprint → `ivansrk/crat-studio` (README, Task 12 Step 4). Нужен аккаунт Render и домен (APP_URL).
2. Автобэкапы Postgres включить в дашборде Render.
3. Smoke-чеклист Ф0: прод-URL открывается (тёмный фон, крупный кегль) · `/health` зелёный · урок-заглушка читается.
4. ⚠️ Параллельно запустить настройку Resend (домен + DKIM) — главный блокер Ф1.

## Self-review

- Spec coverage: CNT-01 (Task 6), CNT-02 (Task 6/10), CNT-03 (Task 4/9 — заглушка урока, не падаем), CNT-04 (Task 8), LES-04/15 (Task 5/7/9), LES-16 (Task 7, prefers-reduced-motion в globals.css), UX-01…04 (Task 2), UX-07 (Task 10), SEC-01/02 (готово ранее + CI gitleaks), SEC-07 (render.yaml), SEC-08 (роуты тонкие, логика в lib/content), AUTH-09-часть (Task 11), seed v0 (Task 11). LES-06 (auth-гейт) — сознательно Ф1, отмечено в Task 9.
- Types: `MdxComponentName` (Task 5) = ключи карты (Task 7) — проверяется typecheck'ом; `ContentIssue`/`CourseContent` согласованы между Task 4/6/10.
